# ── Web / API layer ─────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0          # async HTTP client used by notebooks to hit mock APIs
requests>=2.31.0       # sync HTTP client used in strict-API notebook

# ── MCP ──────────────────────────────────────────────────────────────────────
mcp>=1.0.0             # Official Python MCP SDK (tool registration, stdio/HTTP transport)

# ── LLM / Agent Orchestration ────────────────────────────────────────────────
langchain>=0.2.0
langchain-openai>=0.1.0
langchain-anthropic>=0.1.0   # swap LLM backend via env var
openai>=1.30.0
anthropic>=0.28.0

# ── Jupyter / Notebook ───────────────────────────────────────────────────────
jupyter>=1.0.0
ipykernel>=6.29.0
ipywidgets>=8.1.0
notebook>=7.1.0

# ── Visualization / Output ───────────────────────────────────────────────────
matplotlib>=3.8.0
networkx>=3.3          # decision-trace graph in notebook 03
rich>=13.7.0           # pretty trace printing in notebooks
pandas>=2.2.0
tabulate>=0.9.0        # markdown tables in notebook 03

# ── Utilities ────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0   # load OPENAI_API_KEY / ANTHROPIC_API_KEY from .env
pydantic>=2.7.0        # data models (FastAPI uses this internally)
